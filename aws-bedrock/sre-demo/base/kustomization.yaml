apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Just the workload that is broken on purpose. Consumed as a base by the EKS overlay
# (../) and the k3s overlay (../k3s), so the manifest itself lives in exactly one place.
resources:
  - 01-broken-deployment.yaml
